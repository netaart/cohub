---
"@neta-art/cohub-cli": patch
---

Bundle `sandboxd` `v2.55.0`, keeping the same compatible runner protocol while aligning the CLI download with the `v2.55` platform release.

内置 `sandboxd` 升级至 `v2.55.0`，保持兼容的 runner 协议不变，并让 CLI 下载版本与 `v2.55` 平台版本对齐。
