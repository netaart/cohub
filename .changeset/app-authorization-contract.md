---
"@neta-art/cohub": minor
"@neta-art/cohub-cli": minor
---

Add structured App authorization with explicit targets, viewer-controlled fallback, host capability negotiation, and incremental consent. Preserve legacy authorization return types. Fix logged-out consent, login recovery, canonical App slug resolution, and viewer cache isolation. Add `apps authorize --extend`.

新增结构化 App 授权、明确目标、用户 Space 回退、宿主能力协商与增量授权，保留旧接口返回类型。修复未登录授权、登录恢复、App slug 解析与账户缓存隔离；CLI 新增 `apps authorize --extend`。
