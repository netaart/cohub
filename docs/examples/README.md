# Examples

Sample assets for Cohub guides and demos. Published App samples live in
[`cohub-apps/`](../cohub-apps/); this directory only holds non-App samples.

| Path | What it shows |
|---|---|
| `generations/` | Multimodal generation model declaration samples |
| `workspace-default-layout/` | Default workspace layout template for a Space |

These are reference samples. Paths inside a published App (for example
Space-root file paths) may need adjusting to match how you upload them into a
Space.
